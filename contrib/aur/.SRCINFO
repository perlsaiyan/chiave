# Generated by hand to match PKGBUILD; regenerate with
# `makepkg --printsrcinfo > .SRCINFO` once this package actually builds
# (requires a real published release tarball and checksum -- see the
# UNCERTAIN notes in PKGBUILD).
pkgbase = chiave
	pkgdesc = KeePass KDBX shell and TUI for the terminal (a compiled kpcli successor)
	pkgver = 0.1.0
	pkgrel = 1
	url = https://github.com/perlsaiyan/chiave
	arch = x86_64
	arch = aarch64
	license = MIT
	license = Apache-2.0
	makedepends = cargo
	depends = gcc-libs
	optdepends = wl-clipboard: Wayland clipboard support (wl-copy/wl-paste)
	optdepends = xclip: X11 clipboard fallback
	optdepends = keepassxc: browser extension + ssh-agent integration against the same vault
	provides = chiave
	conflicts = chiave
	source = chiave-0.1.0.tar.gz::https://github.com/perlsaiyan/chiave/archive/refs/tags/v0.1.0.tar.gz
	sha256sums = SKIP

pkgname = chiave
